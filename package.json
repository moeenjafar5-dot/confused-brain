 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/package.json b/package.json
new file mode 100644
index 0000000000000000000000000000000000000000..b6f063b6bdc52ab1c8559fa456412184c5b1be8c
--- /dev/null
+++ b/package.json
@@ -0,0 +1,8 @@
+{
+  "scripts": {
+    "dev": "python3 -m http.server 5173",
+    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/index.html && cp src/app.js src/styles.css dist/src/",
+    "preview": "python3 -m http.server 4173 -d dist"
+  },
+  "devDependencies": {}
+}
 
EOF
)
